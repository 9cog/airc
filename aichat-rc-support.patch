diff --git a/src/utils/command.rs b/src/utils/command.rs
index 1234567..abcdefg 100644
--- a/src/utils/command.rs
+++ b/src/utils/command.rs
@@ -110,6 +110,11 @@ pub fn append_to_shell_history(shell: &str, command: &str, exit_code: i32) -> 
     if let Some(history_file) = get_history_file(shell) {
         let command = command.replace('\n', " ");
         let now = now_timestamp();
+        let history_txt = if shell == "rc" {
+            // rc shell: simple format, just append the command
+            command
+        } else if shell == "fish" {
         let history_txt = if shell == "fish" {
             format!("- cmd: {command}\n  when: {now}")
         } else if shell == "zsh" {
@@ -145,6 +150,12 @@ fn get_history_file(shell: &str) -> Option<PathBuf> {
             .ok()
             .map(PathBuf::from)
             .or(Some(home_dir()?.join(".bash_history"))),
+        "rc" => env::var("history")
+            .ok()
+            .map(PathBuf::from)
+            .or_else(|| Some(home_dir()?.join(".rc_history"))),
         "zsh" => env::var("HISTFILE")
             .ok()
             .map(PathBuf::from)
