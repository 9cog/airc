--- a/src/utils/command.rs
+++ b/src/utils/command.rs
@@ -120,6 +120,9 @@ pub fn append_to_shell_history(shell: &str, command: &str, exit_code: i32) -> 
         let now = now_timestamp();
         let history_txt = if shell == "fish" {
             format!("- cmd: {command}\n  when: {now}")
+        } else if shell == "rc" {
+            // rc shell: simple format, just append the command
+            command
         } else if shell == "zsh" {
             format!(": {now}:{exit_code};{command}",)
         } else {
@@ -150,6 +153,10 @@ fn get_history_file(shell: &str) -> Option<PathBuf> {
             .ok()
             .map(PathBuf::from)
             .or(Some(home_dir()?.join(".bash_history"))),
+        "rc" => env::var("history")
+            .ok()
+            .map(PathBuf::from)
+            .or_else(|| Some(home_dir()?.join(".rc_history"))),
         "zsh" => env::var("HISTFILE")
             .ok()
             .map(PathBuf::from)
